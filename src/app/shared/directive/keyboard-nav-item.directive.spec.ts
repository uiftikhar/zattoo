import { KeyboardNavItemDirective } from './keyboard-nav-item.directive';

describe('KeyboardNavItemDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyboardNavItemDirective();
    expect(directive).toBeTruthy();
  });
});
