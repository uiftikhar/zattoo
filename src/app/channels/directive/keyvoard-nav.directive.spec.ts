import { KeyboardNavDirective } from './keyboard-nav.directive';

describe('KeyboardNavDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyboardNavDirective();
    expect(directive).toBeTruthy();
  });
});
